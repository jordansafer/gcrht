יכולת שלום() {
    בקרה.תעד("שלום עולם! זהו קוד חווה סקריפט!");
    יהי מספר = 42;
    בקרה.תעד("המספר הוא:", מספר);
}

יכולת חישוב(א, ב) {
    יהי סכום = א + ב;
    בקרה.תעד("סכום של", א, "ו-", ב, "הוא", סכום);
    סכום;
}

שלום();
יהי תוצאה = חישוב(10, 20);
בקרה.תעד("התוצאה היא:", תוצאה);
